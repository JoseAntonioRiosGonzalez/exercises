/**
 * @author jose rios
 *
 * Main class application entry point
 */
public class HelloWorld {
    /**
     *
     * Main function entry point for the application
     * @param args argument to receive
     * @since 1.0 // @since is used to indicate which version added the class, method, etc.
     * //if we included @version, this would indicate the current version
     **/
    public static void main(String[] args) {
        System.out.println("Hello world!");
    }
}